# pnpm only. Never npm install or yarn add — the repo convention, and mixing
# lockfiles across the two package managers is how a "works on my machine" build
# starts.
engine-strict=true
strict-peer-dependencies=false
